Algoritmo ejercicio3_guia4
	definir i, vector, n, busc Como Entero
	escribir "Ingrese la cantidad de valores a ingresar"
	leer n
	dimension vector(n)
	
	para i<-0 hasta (n-1) con paso 1 Hacer
		leer vector[i]
	FinPara
	
	escribir "Ingrese el valor a buscar en el vector"
	leer busc
	
	para i<-0 hasta (n-1) con paso 1 Hacer
		Si busc=vector[i] Entonces
			escribir "El valor ", vector[i], "está en la posición ",i
		FinSi
	FinPara
FinAlgoritmo
